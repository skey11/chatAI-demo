type Message {
  id: ID!
  role: String!
  content: String!
  createdAt: String!
}

type Conversation {
  id: ID!
  messages: [Message!]!
}

type Query {
  conversation(id: ID!): Conversation
}

type Mutation {
  sendMessage(conversationId: ID!, content: String!): Message!
}
